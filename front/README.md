# TEDxUNIS Frontend

Requirements:

- Node v18
- NPM

Setup:

```bash
npm install
```

Development:

```bash
npm run dev
```

Build: Create a `.env` file with the `VITE_PROD_API_URL` var pointing to the public production API (no slash at the end)

```bash
npm run build
```
